package com.johnny.main;

import javax.swing.*;

public class GameWindow extends JFrame {

    public GameWindow() {
        setSize(400, 400);
        setVisible(true);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
    }
}
